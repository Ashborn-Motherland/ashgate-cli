#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../postject/dist/cli.js" "$@"
else
  exec node  "$basedir/../../../../postject/dist/cli.js" "$@"
fi
# cmd-shim-target=/home/alexis/Ressources/ashborn/ash-wallet/wallet_cli/node_modules/.pnpm/@yao-pkg+pkg@6.22.0/node_modules/postject/dist/cli.js
